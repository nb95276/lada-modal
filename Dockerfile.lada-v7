# Lada Modal v7 Docker Image
# 基于官方 lada 镜像，添加必要工具
# 构建: docker build -f Dockerfile.lada-v7 -t fkccp/lada-modal:latest .
# 推送: docker push fkccp/lada-modal:latest

FROM ladaapp/lada:latest

USER root

# 安装系统依赖
RUN apt-get update && apt-get install -y --no-install-recommends \
    aria2 \
    && rm -rf /var/lib/apt/lists/*

USER lada

# 安装额外 Python 依赖（使用 --user 与官方镜像保持一致）
RUN pip install --user --no-cache-dir fastapi requests tqdm

ENTRYPOINT ["lada-cli"]
CMD ["--help"]
